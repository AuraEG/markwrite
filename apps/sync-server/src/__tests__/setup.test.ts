// ==========================================================================
// File    : setup.test.ts
// Project : MarkWrite Sync Server
// Purpose : Placeholder test to verify test setup.
// ==========================================================================

import { describe, it, expect } from 'vitest';

describe('Test Setup', () => {
  it('should run tests', () => {
    expect(true).toBe(true);
  });
});
